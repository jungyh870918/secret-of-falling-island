# 프로토타입 검수 결과

명세서 **§23 첫 개발 완료 기준** 16개 항목 + §19 QA 체크리스트.
Phase 2(수직 슬라이스) 기준으로 갱신됐다 — 프롤로그 전 구간, §9 대화 배틀,
§5.4 초상화, §7.2 배경음악 3곡이 검증 범위에 들어와 있다.

검증 환경: **Godot 4.7.1** (macOS, Apple M5, OpenGL Compatibility)

---

## 0. 자동 검사 — 전부 통과

```bash
python3 tests/check_project.py                        # 프로젝트 구조 (Godot 불필요)
python3 tests/validate_data.py                        # 데이터 무결성 (Godot 불필요)
godot --headless --path . tests/SmokeTest.tscn        # 통합 테스트 135건
godot --path . tests/Screenshots.tscn                 # 렌더링 캡처 30장
```

| 검사 | 결과 |
|---|---|
| `check_project.py` | 통과 — GDScript 34개, class_name 26개, autoload 7개 |
| `validate_data.py` | 통과 — 오류 0, 경고 0 |
| `SmokeTest` | **135/135 통과** (3회 반복 동일) |
| GDScript 컴파일 | 오류 0, 경고 0 (Godot 4.7 기본 엄격 설정) |
| 헤드리스 부팅 | 런타임 오류 0 |

---

## 1. §23 완료 기준 16개 — 자동 검증됨

`tests/SmokeTest.gd` 가 프롤로그 전체를 코드로 플레이하며 확인한다.

| # | 항목 | 상태 |
|---|---|---|
| 1 | 타이틀 화면 진입 | ✅ 자동 |
| 2 | 새 게임 시작 | ✅ 자동 |
| 3 | 한개미가 회의실에서 이동 | ✅ 자동 (배치 + walkbox 내부 확인) |
| 4 | 부장과 대화 | ✅ 자동 (§8.1 선택지 4분기 전부 합류 확인) |
| 5 | 탕비실로 이동 | ✅ 자동 |
| 6 | 디카페인 커피 획득 | ✅ 자동 |
| 7 | 인벤토리에서 커피 선택 | ✅ 자동 |
| 8 | 부장에게 사용 | ✅ 자동 |
| 9 | 잘못된 대상 → 고유 실패 대사 | ✅ 자동 (5종 개별 확인) |
| 10 | 퍼즐 해결 후 부장 퇴장 | ✅ 자동 (스프라이트·핫스폿 소멸 확인) |
| 11 | 새로운 출구 활성화 | ✅ 자동 (해결 전 잠김 → 해결 후 통과) |
| 12 | 자동 저장 | ✅ 자동 |
| 13 | 재실행 후 이어하기 | ✅ 자동 (상태 리셋 후 복원 확인) |
| 14 | 픽셀 퍼펙트 확대 | ✅ 캡처 확인 (320×180 원본, 보간 없음) |
| 15 | 창 크기 변경 시 비율 유지 | ⬜ **수동 확인 필요** (창을 드래그해 봐야 함) |
| 16 | 대화 로그 확인 | ✅ 자동 + 캡처 |
| — | 진행 불가 버그 없음 | ✅ 자동 (아래 §3) |

**§19.2 무반응 클릭**: 6개 장면 × 35개 대상 × 8동사 × (맨손 + 아이템 12종)
= **4368개 조합 전부 대사가 출력됨**. 무반응 0건.

**로컬라이징**: 플레이 전 구간에서 누락 키 0건.

---

## 2. 실행 중 발견해 고친 실제 버그

| # | 버그 | 원인 | 수정 |
|---|---|---|---|
| 1 | 장면 파일이 전혀 안 읽힘 | `GameData._merge_keyed` 가 `{"scene_id": ...}` 단일 오브젝트 형태를 처리 못 함 | 세 가지 문서 형태를 모두 지원 |
| 2 | **이어하기가 옛 저장을 염** | 자동 저장이 같은 초에 여러 번 → `saved_at_unix`(초 단위)로 순서 구분 불가 | 단조 증가 `save_index` 도입 |
| 3 | 컷신 문구가 대화 기록에 안 남음 | `CardView` 가 `DialogueLog` 에 기록하지 않음 | 카드 문구도 기록 (§18) |
| 4 | 컷신 중 조작 잠금이 풀림 | `event_requested` 시그널 emit 이 핸들러를 await 하지 않음 | await 가능한 `event_handler` Callable 로 변경 |
| 5 | '즉시' 텍스트 속도가 가장 느림 | `length / max(cps,1)` 에서 cps=0 → 글자 수만큼 초가 됨 | cps=0 이면 타자 시간 0 |
| 6 | 머그컵이 모니터 위에 겹쳐 그려짐 | 좌표 겹침 | 책상을 넓히고 재배치 + 검증기에 기하 검사 추가 |
| 7 | 실패음이 '보다'에서도 울림 | 조사도 실패로 취급 | LOOK 제외 + 결과가 sfx 를 지정하면 존중 |

### 캡처로 발견해 고친 레이아웃 문제

| # | 문제 | 수정 |
|---|---|---|
| 8 | 설정 13항목이 박스를 넘침 | 박스 확대 (18,2,284,176) |
| 9 | 저장 슬롯 13개가 박스 밖으로 흘러넘침 | 박스 확대 + row_h 11 |
| 10 | 대화 기록 마지막 줄이 하단 안내문과 겹침 | 표시 줄 수 계산에 안내문 자리 반영 |
| 11 | 글자 '크게' 에서 동사 버튼끼리 충돌 | 동사 칸 38px + 가운데정렬 클립 + 크기 상한 10px |
| 12 | 타이틀 부제목이 메뉴 박스에 붙음 | 제목/부제목 위로 이동 |
| 13 | 저장 썸네일에 명령 패널이 들어감 | 게임 장면(320×135)만 잘라 64×27 로 축소 |

---

## 3. 진행 불가 시나리오 — 자동 확인됨

- ✅ 컵 관찰 전/후 어느 순서로 진행해도 막히지 않음
- ✅ 라벨 재수령 시도 → 고유 대사, 상태 불변
- ✅ 커피를 싱크대에 버리려 함 → 거부, 아이템 유지 (§11.2)
- ✅ 부장 대화 선택지 소진 → 대화가 짧게 종료, 재대화 가능
- ✅ 부장 퇴장 후 부장 핫스폿 클릭 불가
- ✅ 전제조건 실패 시 아이템 소모 안 됨

---

## 4. 렌더링 캡처

`docs/screenshots/` 에 30장. 전부 320×180 원본 해상도.
(파일 이름은 `tests/Screenshots.gd` 의 캡처 순서와 같다)

**한글 폰트 결론**: `SystemFont` 폴백이 macOS 에서 정상 작동한다.
9~12px 에서 **읽힌다**. 두부(□) 없음. 별도 비트맵 폰트 없이도 프로토타입 진행 가능.
(윈도우/리눅스는 각 OS 폰트로 폴백되므로 해당 환경에서 한 번 더 볼 것)

| 파일 | 확인 내용 |
|---|---|
| `01_title` ~ `04_subtitle_narration` | 타이틀 · 설정 · 인트로 카드 · 자막 |
| `05_room_meeting` ~ `10_panel_simple` | 회의실 · 포커스 · §5.3 문장 라인 · 대화 · §5.2 간소화 UI |
| `11_room_corridor` ~ `13_highlight_on` | 복도 · 핫스폿 강조 OFF/ON 대비 |
| `14_room_pantry_before` ~ `16_fail_line` | 탕비실 (집기 전/후) · §21 고유 실패 대사 |
| `17_log` ~ `20_font_large` | 대화 기록 · 저장 화면 · 디버그 패널 · 글자 크게 |
| **`21_room_subway`** | §8.1 심야 지하철 — 광고판 · 노선도 · 바닥의 전단지 |
| **`22_subway_reflection`** | 창문 반사 — §8.1 '결핍 제시' |
| **`23_room_studio`** | §8.1 한개미의 원룸 |
| **`24_portrait_park`** | §5.4 초상화 대화 — 박프로 (overexposed · tie · wide_smile) |
| **`25_portrait_choices`** | §5.4 초상화 + 선택지 4개가 겹치지 않는지 |
| **`26_room_harbor`** | §8.1 황소항 입구 — 황소 동상 · 전광판 · 환전소 |
| **`27_harbor_sera`** | §8.1 윤세라 첫 등장 — 코트 실루엣과 허리의 금속 자 |
| **`28_portrait_sera`** | §5.4 초상화 대화 — 윤세라 (장식 없음이 이 인물의 디자인) |
| **`29_battle_start`** | §9 대화 배틀 — 상대 초상 · 멘탈 차트 · 군중 |
| **`30_battle_chart`** | §9.4 모순 카드 획득 후 — 캔들이 지지선으로 내려간다 |

## 4b. 플레이 피드백 반영 (1차)

실제 플레이 후 받은 지적 두 가지를 반영했다.

### "반응이 답답하다 / 포커스 깜빡임이 너무 길다"

| 항목 | 이전 | 이후 |
|---|---|---|
| 마우스오버 포커스 | 0.6초 주기 점멸 (절반은 안 보임) | **상시 표시**. 점멸은 눈이 아프고 "반응이 없나?" 하는 착각을 준다 |
| 커서 | 핫스폿 위에서 0.5초 주기 점멸 | 상시 강조 + 십자가 조여드는 형태 변화 |
| `보다` 동작 | 대상까지 걸어간 뒤 대사 (최대 2~3초) | **걷지 않는다.** 고개만 돌리고 즉시 대사 |
| 걷는 중 클릭 | 무시됨 | **새 목표로 즉시 전환** (진행 중 행동 취소) |
| 이동 속도 | 62 px/s | 90 px/s |
| 자막 (30자, '보통') | 3.4초 | 1.8초 |
| 장면 전환 암전 | 264ms | 120ms |

`보다`만 즉시 반응하게 한 이유: 눈으로 하는 동작은 거리와 무관하고, 조사가
가장 빈번한 행동이라 여기서 지연이 체감의 대부분을 만든다. 물건을 집거나
쓰는 동작은 여전히 걸어간다 — 그게 없으면 공간감이 사라진다.

### "독백이 너무 이성적이다 / 투덜대게"

한개미 대사 **165개를 전면 재작성**했다. 투덜대고, 감정이 먼저 나오고,
자기합리화하고, 종결어미가 짧다.

```
이전: "디카페인 원두를 챙겼다. 카페인만 빠진 커피. 겉으로는 똑같다."
이후: "일단 챙겼다."
      "카페인만 뺐다는데 그럼 뭐가 남는 거야. …아니 잠깐. 겉으로는 똑같잖아."
```

손대지 않은 것:
- `narrator` 대사 — §8.1 원문 인용이고, 투덜대는 한개미와 대비되어야 여운이 남는다
- §8.1 선택지 4개, §10.2 사원증 설명 — 명세서 원문
- 날카로운 한 방 2~3개 (§0-6 "무능한 바보가 아니다", §24 "한 박자 늦게 깨닫는 농담")
  예: "방송 차트랑 우리 매출표가 똑같이 생겼다. 둘 다 오른쪽이 비어 있네."

---

## 4c. 플레이 피드백 반영 (2차)

### "대사가 뜨는 속도가 너무 빨라"

1차에서 "반응이 답답하다"를 클릭 반응 지연으로 읽고 타자 속도까지 같이 올렸는데
그게 과했다. 둘을 분리했다 — **글자가 찍히는 속도는 느리게, 다 찍힌 뒤
붙잡는 시간은 짧게.**

| | 원래 | 1차(과했음) | 2차 |
|---|---|---|---|
| 타자 속도 (보통) | 26 cps | 42 cps | **20 cps** |
| 30자 대사가 찍히는 시간 | 1.15초 | 0.7초 | **1.5초** |
| 다 찍힌 뒤 홀드 | 2.25초 | 1.12초 | 1.23초 |
| 합계 | 3.4초 | 1.8초 | **2.7초** |

### "대사 글씨를 조금 더 도트느낌 나게"

`SystemFont`(Apple SD Gothic Neo)는 크기만 작을 뿐 곡선이 매끄러운 현대
고딕이라 도트 느낌이 안 났다. **Galmuri** 한글 도트 폰트(OFL 1.1)로 교체했다.
Godot 임포터가 스스로 "Pixel font detected"로 인식하고 subpixel/hinting 을
끈다 — 진짜 비트맵 설계 폰트라는 확인이다.

핵심은 **설계 크기로만 쓰는 것**이다. Galmuri11 을 9px 로 줄이면 리샘플링되어
도트가 뭉개진다. 그래서 크기 단계마다 폰트를 따로 둔다(9/11/14px).
`Theming.small_font` 도 같은 이유로 추가했다 — 좁은 칸에서 크기만 줄이면
안 되고 더 작은 폰트로 갈아타야 한다.

### 폰트 교체로 드러난 레이아웃 문제

11px 폰트는 이전 10px보다 크므로 촘촘한 목록이 깨졌다.

| 문제 | 수정 |
|---|---|
| 설정 13항목이 행 높이 11px에 11px 글자로 붙어버림 | `MenuList.dense` 추가 — 작은 도트 폰트(9px) + 행 12px. 제목만 본문 크기 유지 |
| 저장 슬롯 13개도 같은 문제 | 같은 처리. 썸네일 정보 폭도 넓혀 저장 시각이 안 잘리게 |
| 타이틀 부제목이 양초 위에서 금색이라 묻힘 | 아이보리 + 외곽선. PNG 픽셀을 직접 샘플링해 검증했다 |
| Godot 이 `docs/screenshots/` 를 게임 에셋으로 임포트 | `.gdignore` 추가 |

---

## 5. 남은 수동 확인 항목

자동화할 수 없는 것들. 실제로 플레이하며 봐 주세요.

- ⬜ **§23-15 창 크기 변경 시 비율 유지** — 창을 드래그해 레터박스 확인
- ⬜ **조작감** — 클릭 지점과 핫스폿이 실제로 맞는지, 걷기 속도가 답답하지 않은지
- ⬜ **효과음** — 런타임 합성 펄스파가 실제로 들리는지, 거슬리지 않는지
- ⬜ **대사 템포** — 자동 넘김 시간이 읽기에 충분한지
- ⬜ **재미** — §3.2 "MVP가 재미있어야 전체 제작으로 확장한다"

### 알려진 무해한 경고

종료 시 `6 ObjectDB instances were leaked` — 합성한 `AudioStreamWAV` 가
오디오 서버 종료 순서 때문에 남는다. 게임 동작에는 영향이 없다.

---

## 6. Phase 2 — 수직 슬라이스 (§20)

프롤로그가 §8.1 의 다섯 장소 전부로 늘어났고, §9 대화 배틀·§5.4 초상화·§7.2
배경음악이 붙었다.

| Phase 2 항목 (§20) | 상태 |
|---|---|
| 1. 프롤로그 전체 | ✅ 회의실 · 복도 · 탕비실 · 지하철 · 원룸 · 황소항 입구 |
| 2. 황소항 입구 | ✅ 황소 동상 · 전광판 · 게이트 · 가짜 환전소 |
| 3. 한개미와 세라 정식 스프라이트 | ⚠️ **절충** — 아래 참고 |
| 4. 초상화 대화 | ✅ 96×96, 분기 대화·배틀에서만. 에셋 교체 지점 준비됨 |
| 5. 첫 대화 배틀 | ✅ 환전소 3페이즈, 약점 유형 `self_contradiction` |
| 6. 배경음악 3곡 | ✅ `office_night` · `bull_harbor` · `sera` (런타임 합성) |
| 7. 설정 메뉴 | ✅ Phase 1 에서 완료. 항목 13개로 늘어남 (초상화 on/off 추가) |
| 8. 30~45분 플레이 가능 빌드 | ⬜ **수동 측정 필요** — 아래 참고 |

### 3번이 절충인 이유

"정식 스프라이트"는 손으로 그린 도트 에셋을 뜻한다. 그건 코드가 만들 수 있는
물건이 아니라서, 대신 **임시 캐릭터의 품질을 올렸다**.

- §6.6 "걷기: 방향당 6프레임" 을 실제 6프레임 포즈 표로 구현 (`Actor.WALK_LEG` 등)
- 대기 3프레임(숨), 말하기 2프레임(입)
- 옆모습에서 다리가 앞뒤로 겹치고 팔이 반대로 흔들림
- 뒤를 보면 얼굴이 사라짐 — 작은 도트에서 방향을 알리는 가장 확실한 신호
- §6.7 개별 특징: 한개미 걷은 소매 + 가방, 세라 긴 코트 + 허리의 금속 자, 부장 대머리

`assets/sprites/characters/<id>.png` 를 넣으면 이 코드는 통째로 비활성화된다.
즉 **에셋이 들어올 자리는 그대로 비어 있고, 그때까지의 품질만 올린 것**이다.

### 8번을 자동으로 못 재는 이유

플레이 타임은 사람이 읽고 헤매는 시간이라 코드로 측정하면 의미가 없다.
스모크 테스트는 최단 경로를 즉시 넘기며 도는 것이라 참고가 안 된다.
콘텐츠 규모는 다음과 같다 — 실제 측정은 한 번 플레이해 봐야 한다.

| | Phase 1 | Phase 2 |
|---|---|---|
| 장면 | 3 | **6** |
| 핫스폿·출구 | 18 | **41** |
| 아이템 | 7 | **12** |
| 분기 대화 | 4 | **12** |
| 퍼즐 | 1 | **3** |
| 대화 배틀 | 0 | **1** (3페이즈) |
| 상호작용 룰 | 47 | **129** |
| 문장 | 354 | **823** |

---

## 7. Phase 2 에서 실행 중 고친 것

| # | 문제 | 원인 | 수정 |
|---|---|---|---|
| 14 | 로컬라이징 파일을 나누자 `_comment` 중복 키 오류 | `GameData._merge_flat` 이 주석 키까지 중복으로 셈 | `_` 로 시작하는 키는 건너뛴다 (다른 로더와 같은 규약) |
| 15 | 배틀 HUD 의 '군중'·'모순' 라벨이 서로 겹침 | 행 높이 9px 에 11px 글자 | 행을 13px 로 넓히고 각 행에 배경판. 라벨 왼쪽·내용 오른쪽 한 줄 배치 |
| 16 | 배틀 HUD 가 배경 위에 떠서 안 읽힘 | 배경판 없음 | 차트와 같은 어두운 판 + 1px 외곽선 |
| 17 | 전단지를 가진 채 노트북을 쓰면 "볼 게 없다" | 소지 여부를 안 보는 룰 하나뿐 | 소지 중 전용 룰 추가 — 인벤토리에서 꺼내라고 알려 준다 |

### 검증기에 추가한 검사

Phase 2 에서 새로 생긴 실수 유형을 검증기가 잡도록 했다.

- **화자 키를 데이터에서 수집** — 하드코딩 목록을 지웠다. 새 인물을 넣으면
  `speaker.<id>` 가 없을 때 오류로 잡히고, 캐릭터 정의가 없으면 경고한다.
- **§9 배틀 검사** — 약점 유형이 §9.3 의 10종인지, 페이즈마다 선택지가 3~4개인지
  (조건부 선택지를 빼고도 3개인지), 자신감을 깎는 선택지가 하나는 있는지,
  지지선이 초기 자신감 안에 있는지, 힌트가 실재하는 페이즈를 가리키는지.
- **승패가 같은 곳으로 나아가는지** — `on_win` 과 `on_lose` 의 `puzzle` op 이
  다르면 오류다. §9.5 "오답을 선택해도 즉시 패배하지 않는다" 를 구조로 강제한다.

---

## 8. 다음 작업 전 확정할 것

- ⬜ 30~45분 빌드인지 실제로 재 보기 (§20 Phase 2-8)
- ⬜ 배경음악 3곡이 60초 루프에서 지겹지 않은지 — 반복해 들어 보기
- ⬜ 초상화 96×96 이 화면의 3분의 1을 덮는 게 과한지 (설정에서 끌 수 있게는 해 뒀다)
- §20 Phase 3 착수 — 챕터 1 투자자 조합 세 시험, 대화 배틀 3개.
  §9.6 차트도사 구봉 배틀은 명세서에 대본이 이미 있다.
